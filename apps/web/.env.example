# ============================================
# KRG e-Visit System - Web App Environment Variables
# ============================================

# API Configuration
# This tells the frontend where to find the backend API
# Development: http://localhost:3001
# Production: https://your-api.railway.app (or your actual API URL)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Application Configuration
NEXT_PUBLIC_APP_NAME=KRG e-Visit System
NEXT_PUBLIC_DEFAULT_LOCALE=en

# ============================================
# NOTES
# ============================================
# 1. Copy this file to .env.local in the same directory
# 2. All NEXT_PUBLIC_* variables are exposed to the browser
# 3. Never put secrets here - they will be visible in client-side code
# 4. Update NEXT_PUBLIC_API_URL for production deployment
# 5. .env.local is gitignored by default
